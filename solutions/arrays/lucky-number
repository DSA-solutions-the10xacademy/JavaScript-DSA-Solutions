let fs = require("fs");
let data = fs.readFileSync(0, 'utf-8');
let idx = 0;
data = data.split('\n');

function readLine() {
	idx++;
	return data[idx - 1];
}

// -------- Do NOT edit anything above this line ----------

function findLuckyNumber(nums) {

	let lastElement = -1, numberOfOccurences = 0;
	for (let index = 0; index < nums.length; index++) {
		if (nums[index] === lastElement) {
			numberOfOccurences++;
		}
		else {
			lastElement = nums[index];
			numberOfOccurences = 1;
		}
		if (numberOfOccurences === lastElement)
			return numberOfOccurences;
	}
	return -1;

}
// DO NOT change anything below this line
let num_elems = parseInt(readLine())
let input_arr = []
for (let index = 0; index < num_elems; index++) {
	input_arr.push(parseInt(readLine()))
}
console.log(findLuckyNumber(input_arr))
